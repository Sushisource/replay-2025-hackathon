import { VerifyConfig, VerifyOutput } from "./verify.types";

export const verifyConfig: VerifyConfig = {
  path: "ts-node",
  options: "",
};

export const initialVerifyOutcome: VerifyOutput = {
  success: false,
  output: "",
};
